python的thread模块这个模块是比较底层的一个模块,python的threading模块对thread模块做了包装





> **def **word():
>
>     <font style="color:#000080;">print</font>(**"****写文档****"**)
>
>     time.sleep(<font style="color:#0000ff;">1</font>)
>
> 
>
> **if **__name__ == **'__main__'**:
>
>     **for **i **in **<font style="color:#000080;">range</font>(<font style="color:#0000ff;">5</font>):
>
>         t=threading.Thread(<font style="color:#660099;">target</font>=word)
>
>         t.start() _#__启动线程_
>

使用多线程并发操作,花费的时间比单线程要短很多



<h4 id="pqZpp">threading模块我们能完成多线程程序的开发,为了让每个线程的封装性更完美,那么我们使用这个模块的时候,我们需要定义一个新的子类class只要继承 threading.Thread就可以了,然后我们重写run</h4>


- [ ] <font style="color:#F5222D;">p</font>**<font style="color:#F5222D;">ython中的threading.Thread类有一个run方法,这个方法是用于定义线程的功能函数,可以自己在线程类中覆盖该方法,创建自己的线程实例后,通过thread类的start方法来启动线程,交给python虚拟机进行调度,当该线程获得执行的机会的时候就会调用我们的run方法来执行线程</font>**

**线程的执行顺序**

我是Thread-3@ 0

我是Thread-5@ 0

我是Thread-4@ 0

我是Thread-1@ 0

我是Thread-2@ 0

我是Thread-6@ 0

我是Thread-1@ 1

我是Thread-3@ 1

我是Thread-2@ 1

我是Thread-5@ 1

我是Thread-4@ 1

我是Thread-6@ 1

我是Thread-3@ 2

我是Thread-2@ 2

我是Thread-1@ 2

我是Thread-4@ 2

我是Thread-5@ 2

我是Thread-6@ 2

<h4 id="RhaTF">说明:可以看出线程的执行顺序是不稳定的,当执行到sleep语句的时候线程将被阻塞,到sleep结束之后线程进入就绪状态,等待调度,而线程将自行选择一个线程执行,从上面的代码可以看出,只能保证线程都完成整个run函数的运行,但是线程的启动书序,run函数中每次循环的执行顺序是不能确定的</h4>




**1.每个线程默认都有自己的一个名字尽管我们的例子中没有指定线程对象的name但是python会自动为线程指定一个名字**

**2.当线程的run方法结束时线程结束完成**

**3.无法控制线程的调度程序,但是我们可以通过别的方式来影响线程的调度方式**

